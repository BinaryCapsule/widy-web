import { theme } from '@binarycapsule/ui-capsules';

export const GENERIC_ERROR_MSG = 'Oops, something went wrong';

export const SERIES_COLORS = ['blue300', 'blue600'] as (keyof typeof theme.colors)[];
